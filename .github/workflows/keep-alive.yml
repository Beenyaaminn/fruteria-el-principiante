name: Keep Neon Database Alive

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keep-alive endpoint
        run: curl -sf https://fruteriaelprincipiantecl.vercel.app/api/keep-alive
